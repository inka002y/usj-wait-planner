# Optional: only needed after Supabase cloud collection is wired up.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Local web preview only. Native iPhone/Expo Go does not need this proxy.
USJ_DEV_PROXY_PORT=8787
